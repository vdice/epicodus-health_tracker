<%= render 'layouts/errors', object: object %>

<%= form_for object, remote: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.fields_for :calorie_count do |ff| %>
    <%= ff.label :count %>
    <%= ff.text_field :count %>
  <% end %>
  <%= f.submit %>
<% end %>
